// Shared Sanity configuration
// Centralized to avoid duplication across client, CLI, and studio configs

export const sanityConfig = {
  projectId: 'q8bq1hip',
  dataset: 'production',
  apiVersion: '2024-01-19',
} as const
